# BACKLOG

- Reconcile the remaining aspirational sections in `project-draft.md` with the shipped product shape. The checklist now reflects current status and the notification feature is marked out of scope, but the lower config/project-layout sections still describe the old `[general]` / `[worktree]` config model and a native `internal/worktree/` package that do not match the current `worktrunk`-backed implementation.
- Reconcile the stale HANDOFF gap summary with the shipped worktree implementation. [`HANDOFF.md`](/home/ramtinj/personal-workspace/Colosseum/HANDOFF.md) correctly marks managed worktree creation and ownership-aware deletion as done in the feature tables, but a later “Known Issues And Important Gaps” bullet still says roadmap worktrees are absent. That contradiction makes the handoff less trustworthy for the next agent reading the high-level gap summary.
- Refresh the stale repo metrics table in `HANDOFF.md`. The current tree has 47 non-test Go files under `cmd/` + `internal/`, 20 test files, 116 `Test*` functions, 12 Go packages, and 35 fixture files, but the handoff still reports older counts. That makes the “current snapshot” section look more trustworthy than it is.
- Handle state-dir creation failures eagerly in the CLI bootstrap. [`cmd/colosseum/bootstrap.go`](/home/ramtinj/personal-workspace/Colosseum/cmd/colosseum/bootstrap.go) still calls `os.MkdirAll(dir, 0o755)` inside `newStore()` and ignores the returned error. [`internal/workspace/storage.go`](/home/ramtinj/personal-workspace/Colosseum/internal/workspace/storage.go) retries directory creation on writes and surfaces that failure there, but bootstrap itself still drops the original error and read-only commands can advance past initialization without exposing the failed eager setup.
- Tighten Aider waiting detection before trusting it on live panes. [`internal/agent/aider.go`](/home/ramtinj/personal-workspace/Colosseum/internal/agent/aider.go) still treats any line ending in `?` as `Waiting`. That is broader than the recent Claude/Codex waiting hardening and will likely misclassify normal conversational output until Aider gets a narrower prompt/approval signal strategy.
- Remove the vestigial `keys.mark_read` config slot and keybinding now that notifications are explicitly out of scope. The `UnreadCount` field and unread badge rendering are already gone, so keeping a dead notification shortcut in the config/help surface only advertises a feature the product no longer wants.
- Avoid treating unsent composer text as agent errors in status detection. [`internal/status/detector.go`](/home/ramtinj/personal-workspace/Colosseum/internal/status/detector.go) checks `ErrorPatterns` against all recent non-empty pane content once the bottom line stops looking like a bare prompt, and [`internal/agent/patterns.go`](/home/ramtinj/personal-workspace/Colosseum/internal/agent/patterns.go) uses broad matches like `rate limit`, `fatal error`, and `authentication failed`. If broadcast leaves a prompt sitting unsent in the composer, those words can come from the draft itself and falsely flip a workspace to `Error` even though the agent never executed anything.
