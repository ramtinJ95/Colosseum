# BACKLOG

- Make the new-workspace dialog mode-sensitive and use user-facing labels. [`internal/tui/dialog/new_workspace.go`](/home/ramtinj/personal-workspace/Colosseum/internal/tui/dialog/new_workspace.go) always renders every field and exposes raw internal values like `existing-checkout`, `new-worktree`, and `selected-agent`. That makes mode-specific inputs like `Prompt`, `Count`, and `Strategy` look relevant even when they are ignored, and it hides the intended difference between attaching to an existing checkout versus creating a managed worktree.
- Remove or wire up the stray Git inspector before it drifts further. [`internal/workspace/git.go`](/home/ramtinj/personal-workspace/Colosseum/internal/workspace/git.go) defines a `GitInspector` helper and `runGit()` wrapper, but no production code references it. Leaving it around unused makes the new worktree backend harder to understand because it suggests a second Git integration path that is not actually part of the shipped design.
- Handle state-dir creation failures in the CLI bootstrap. [`cmd/colosseum/bootstrap.go`](/home/ramtinj/personal-workspace/Colosseum/cmd/colosseum/bootstrap.go) calls `os.MkdirAll(dir, 0o755)` inside `newStore()` and ignores the returned error. If the config directory is unwritable, later store operations fail farther away from the cause and the CLI never reports the actual directory-creation problem.
- Tighten Aider waiting detection before trusting it on live panes. [`internal/agent/aider.go`](/home/ramtinj/personal-workspace/Colosseum/internal/agent/aider.go) still treats any line ending in `?` as `Waiting`. That is broader than the recent Claude/Codex waiting hardening and will likely misclassify normal conversational output until Aider gets a narrower prompt/approval signal strategy.
- The `keys.mark_read` config slot and its keybinding registration survive, but the `UnreadCount` field and rendering code that gave it meaning have been removed. Either remove the keybinding entirely or repurpose it for a future feature so the config surface does not advertise a dead action.
- Avoid treating unsent composer text as agent errors in status detection. [`internal/status/detector.go`](/home/ramtinj/personal-workspace/Colosseum/internal/status/detector.go) checks `ErrorPatterns` against all recent non-empty pane content once the bottom line stops looking like a bare prompt, and [`internal/agent/patterns.go`](/home/ramtinj/personal-workspace/Colosseum/internal/agent/patterns.go) uses broad matches like `rate limit`, `fatal error`, and `authentication failed`. If broadcast leaves a prompt sitting unsent in the composer, those words can come from the draft itself and falsely flip a workspace to `Error` even though the agent never executed anything.
- Align the create-dialog vocabulary and defaults with the documented create flows. [`internal/tui/dialog/new_workspace.go`](/home/ramtinj/personal-workspace/Colosseum/internal/tui/dialog/new_workspace.go) renders a modal titled `New Workspace` even when `experiment-run` is selected, always shows fields whose meaning changes by mode, and defaults the `Strategy` selector to `selected-agent` because that enum is listed first. [`README.md`](/home/ramtinj/personal-workspace/Colosseum/README.md) documents experiment creation separately and says the CLI default for experiment agents is `all-supported`, while [`worktree-plan.md`](/home/ramtinj/personal-workspace/Colosseum/worktree-plan.md) frames standalone-vs-experiment as the top-level user choice. The current TUI wording makes it too easy to misread `Path`, `Strategy`, and `Count`, and the default experiment behavior does not obviously match the README.
- Hide or disable experiment-only fields outside experiment mode in the new-workspace dialog. [`internal/tui/dialog/new_workspace.go`](/home/ramtinj/personal-workspace/Colosseum/internal/tui/dialog/new_workspace.go) now always renders strategy, prompt, and candidate-count controls even when the user is creating an attached checkout or a single managed worktree. Keeping irrelevant controls visible makes the primary path harder to scan and increases the chance that mode-specific values drift without any effect.
